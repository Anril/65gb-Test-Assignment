<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anril.presentation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".App">
        <activity android:name=".activities.specialties.SpecialtiesActivity"
            android:label="@string/act_title_specialties">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.personslist.PersonsListActivity"
            android:label="@string/act_title_persons"/>

        <activity android:name=".activities.persondetail.PersonDetailActivity"
            android:label="@string/act_title_person_detail"/>
    </application>

</manifest>